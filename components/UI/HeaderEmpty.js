import React from "react";
import { HeaderButton } from "react-navigation-header-buttons";

const HeaderEmpty = (props) => {
  return <HeaderButton {...props} color="#fff" />;
};

export default HeaderEmpty;
